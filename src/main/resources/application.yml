spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study
    username: root
    password: 
  sql:
    init:
      mode: always


# 特定のパッケージのログレベルをDEBUGに設定
logging:
  level:
    com:
      jp:
        study: DEBUG
